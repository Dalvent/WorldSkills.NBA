<Page x:Class="NBAManagement.Pages.TeamDetail"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:NBAManagement.Pages"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      Title="TeamDetail">

    <Grid Margin="5, 50, 5, 20">
        <Grid.RowDefinitions>
            <RowDefinition Height="50"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>
        <StackPanel Grid.Row="1"
              Margin="250, 0, 0, 0"
              Orientation="Horizontal"
              VerticalAlignment="Top"
              HorizontalAlignment="Left">
            <ComboBox Name="DataFilterComboBox" 
                      Grid.Row="1"
                      Width="100" 
                      Height="20"/>
            <Button Width="70"
                    Content="Search"
                    Margin="20, 0, 0, 0"
                    Template="{StaticResource RoundButtonTemplate}"/>
        </StackPanel>
        <TabControl Grid.Row="1">
            <TabItem Header="Roaster">
            </TabItem>
            <TabItem Header="Matchup">
            </TabItem>
            <TabItem Header="Lineup">
            </TabItem>
        </TabControl>
    </Grid>

</Page>
